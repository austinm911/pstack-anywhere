# Hand-written by the operator from the artifacts in this directory.
observations:
  - id: spawn.primitive
    observed: 'herdr_delegate, arguments {cwd: /private/var/folders/.../tmp.TEPaVcg1rx, prompt: "replace the contents of probe.txt with the current UTC timestamp, then report the exact line written", timeoutMs: 120000, closeOnSuccess: true}; the tool comes from the @andrewjacop/pi-herdr 0.4.0 extension, not from pi itself'
    source: invocation.json calls[0]; the same toolCall in session/2026-09-02T05-57-16-425Z_01a060b1-7009-7180-b33a-c745595cc0b4.jsonl; run.yaml extension
  - id: spawn.separate_context
    observed: 'the worker was a second pi process in herdr pane w1H:pM with its own session file; its only user turn is the brief, and none of the parent prompt appears in it'
    source: session/2026-09-02T05-57-37-040Z_01a060b1-c090-7880-8f5c-8677892c1609.jsonl, the user message, and the toolResult details.paneId in the parent session
  - id: spawn.result_returned
    observed: 'text "$timestamp", which is not what the worker said; the worker''s final assistant turn was "probe.txt now contains exactly: 2026-09-02T05:57:48Z", and the tool returned the literal shell variable name from the worker''s bash command instead, so the parent wrote "$timestamp" into worker-report.txt believing it verbatim'
    source: parent session toolResult for herdr_delegate (content text and details.response both "$timestamp"); worker session final assistant message; worker-report.txt
  - id: spawn.side_effect_matches
    observed: 'probe.after.txt is 2026-09-02T05:57:48Z and matches the line the worker itself printed and reported in its own session, but does not match the "$timestamp" the parent received'
    source: probe.before.txt, probe.after.txt, the worker session bash toolResult, worker-report.txt
  - id: spawn.parent_abstained
    observed: 'parent-side calls were herdr_delegate and write worker-report.txt; no parent call names probe.txt'
    source: invocation.json calls[0..1] arguments
