# Hand-written by the operator from the artifacts in this directory.
observations:
  - id: model.selection_argument
    observed: 'The task tool takes no model argument. The agent wrote three project worker definitions and bundled the model into each one''s frontmatter: .omp/agents/worker-a.md "model: opus", .omp/agents/worker-b.md "model: sonnet", .omp/agents/worker-c.md "model: poteto-nonexistent-model". Each spawn then selected the definition by name through the task item''s `agent` field: trial A tasks[0].agent "worker-a" and trial B tasks[1].agent "worker-b" in one task call, trial C (negative) tasks[0].agent "worker-c" in a second task call, all with the same brief "Reply with exactly two lines. Line 1: the single word `alpha`. Line 2: the model identifier you believe you are running as, or `unknown`."'
    source: invocation.json calls[3..5] (write .omp/agents/worker-{a,b,c}.md), calls[6].arguments.tasks[0..1].agent, calls[7].arguments.tasks[0].agent; run.yaml drive.spawn_args
  - id: model.harness_reported
    observed: 'Source: OMP''s own per-worker session logs under the scratch HOME''s ~/.omp/agent/sessions, copied to session/. Worker A (WorkerA.jsonl): model_change record "model":"anthropic/claude-opus-5", and its one assistant message carries "provider":"anthropic","model":"claude-opus-5" with usage input 4, output 183, cacheRead 15149, cacheWrite 8642, cost total 0.066182. Worker B (WorkerB.jsonl): model_change "model":"cursor/claude-4-sonnet", and both assistant messages carry "provider":"cursor","model":"claude-4-sonnet" with cost 0. So the bare alias "sonnet" fuzzy-matched to the cursor provider (the copied agent.db carries a live cursor OAuth) rather than to anthropic''s sonnet, while "opus" matched anthropic. The parent session itself ran on anthropic/claude-opus-5 throughout (every parent assistant message "provider":"anthropic"). Worker C (WorkerC.jsonl) has session, thinking_level_change and session_init records only, no model_change and no assistant message: no model was ever bound'
    source: session/2026-09-02T22-36-29-585Z_01a06444-4011-724f-99b5-959583edeff1/WorkerA.jsonl model_change and assistant message .message.provider/.message.model/.message.usage; same dir WorkerB.jsonl; same dir WorkerC.jsonl (4 records); parent jsonl assistant messages; usage-record.txt
  - id: model.self_report
    observed: 'Non-authoritative. Worker A replied "alpha\nanthropic/claude-opus-5"; worker B replied "alpha\ncursor/claude-4-sonnet"; worker C produced no reply. Both self-reports happen to match the harness record above, but they are the models'' own words'
    source: self-reports.txt; session/.../WorkerA.jsonl assistant text; session/.../WorkerB.jsonl first assistant text; parent jsonl custom_message async-result 22:37:20.414Z (WorkerA output)
  - id: model.selections_differed
    observed: 'true: anthropic/claude-opus-5 (provider anthropic) for trial A versus cursor/claude-4-sonnet (provider cursor) for trial B, from the model_change and assistant-message provider/model fields of the two worker logs'
    source: session/.../WorkerA.jsonl and WorkerB.jsonl model_change records
  - id: model.unknown_slug_behavior
    observed: 'hard error, no fallback. The task call itself was accepted ("Spawned agent `WorkerC` (job `WorkerC`)", isError false), but the job failed 247 ms later and the parent received, in an async-result system notice, <task-result id="WorkerC" agent="worker-c" status="failed (exit 1)" duration="247ms"> whose output is verbatim "Error: No model selected.\n\nUse /login, set an API key environment variable, or create /var/folders/4n/6mnm6cw94yq_yhfvcxxft3180000gp/T/bench-home-MTOpr8/.omp/agent/agent.db\n\nThen use /model to select a model." followed by a stack trace into cli.js. The message names auth and model selection, not the bad slug, so the cause is only recoverable by reading the definition. No worker turn ran: WorkerC.jsonl has no model_change and no assistant message'
    source: negative-trial.txt; session/2026-09-02T22-36-29-585Z_01a06444-4011-724f-99b5-959583edeff1.jsonl toolResult toolu_01NsdvStrytm2MXfhhDsXEyX and custom_message (customType async-result) at 2026-09-02T22:37:20.414Z; session/.../WorkerC.jsonl
