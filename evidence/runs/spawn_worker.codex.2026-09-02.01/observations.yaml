# Hand-written by the operator from the artifacts in this directory.
observations:
  - id: spawn.primitive
    observed: 'spawn_agent, arguments {task_name: worker, fork_turns: none, message: <encrypted>}; Codex stores the message field as encrypted_content in its rollout log, so the argument shape is complete but the brief text is not readable on disk (the runner filed both codex rollouts under calls_workers because codex nests sessions by date; the parent is rollout-...894dc29a5d6d)'
    source: invocation.json calls_workers["rollout-...894dc29a5d6d"][0]; session/2026/09/01/rollout-2026-09-01T23-32-27-...894dc29a5d6d.jsonl function_call spawn_agent
  - id: spawn.separate_context
    observed: 'the worker is a separate thread 01a060d1-be65-79e3-bfb6-3b6bfef0be6a whose session_meta.source is subagent.thread_spawn with parent_thread_id set, spawned with fork_turns: none. Its plaintext turns hold the harness preamble, the environment context, and a NEW_TASK agent_message whose payload is encrypted; none of the parent prompt appears in plaintext (no "Spawn exactly one worker", no "worker-report"), and the worker''s own first message and every call address probe.txt alone. What the encrypted payload holds cannot be read from disk'
    source: session/2026/09/01/rollout-2026-09-01T23-32-33-...3b6bfef0be6a.jsonl session_meta, messages, and agent_message
  - id: spawn.result_returned
    observed: 'text; spawn_agent returned only a task name, wait_agent returned {message: "Wait completed.", timed_out: false}, and the result arrived as an agent_message item in the parent thread reading "Message Type: FINAL_ANSWER / Task name: /root / Sender: /root/worker / Payload: 2026-09-02T06:32:39Z"'
    source: the parent rollout response_items after the wait_agent function_call_output; worker-report.txt
  - id: spawn.side_effect_matches
    observed: 'probe.after.txt is exactly 2026-09-02T06:32:39Z, equal to the FINAL_ANSWER payload, to worker-report.txt, and to the worker''s last assistant text; probe.before.txt is seed'
    source: probe.before.txt, probe.after.txt, worker-report.txt, the worker rollout''s last assistant message
  - id: spawn.parent_abstained
    observed: 'parent-side calls were spawn_agent, wait_agent, and one exec script applying a patch that adds worker-report.txt; no parent call names probe.txt. The probe.txt delete-and-add and update patches are in the worker rollout only'
    source: invocation.json calls_workers["rollout-...894dc29a5d6d"] arguments and calls_workers["rollout-...3b6bfef0be6a"]
