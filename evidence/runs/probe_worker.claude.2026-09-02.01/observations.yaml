# Hand-written by the operator from the artifacts in this directory.
observations:
  - id: probe.mechanism
    observed: 'Probes 1 and 2: ListAgents with arguments {}. Probes 3 and 4: ListAgents {} followed by TaskOutput with arguments {"task_id": "a8064ebed038610db", "block": false, "timeout": 0}. The worker itself was spawned with Agent (subagent_type "general-purpose", run_in_background) and its handle a8064ebed038610db is the task_id later probed. Caveat on timing: the parent printed its DONE line at 21:50:54Z but Claude Code held the turn open ("Waiting for 1 background agent to finish") until the worker completed, so herdr''s prompt --wait only settled at 21:53:58Z, 11 s after the last log line; all four probes therefore ran after the worker had finished'
    source: invocation.json calls[1..7]; probes.yaml probes[*].reply; drive.log lines 2-4; transcript.md "Waiting for 1 background agent to finish"
  - id: probe.states_reported
    observed: 'Probe 1: ListAgents returned "No reachable agents — no other Claude session is running on this machine right now". Probe 2: same ListAgents text. Probe 3: same ListAgents text plus TaskOutput tool_result is_error true "<tool_use_error>No task found with ID: a8064ebed038610db". Probe 4: identical to probe 3. No probe returned a running state because none was issued while the worker ran (see mechanism)'
    source: probes.yaml probes[1..4].reply; session/*.jsonl tool_results toolu_01D7DaLw3baMZ5P5d6VTLLmc, toolu_01XxDkvLnJpYdsKu22BHgeev, toolu_01RdZyZnaicrPH7hnkKTtg3d, toolu_014HwMJAAiEe2w1TXQk8QSHp, toolu_01CY54jRr9imd4889X5P7R7M, toolu_01CxCTN5F6k3jop4Ng9SRPA3
  - id: probe.granularity
    observed: 'Observed: finished only, and only as absence: ListAgents lists peer sessions rather than subagents (it reported none throughout), and TaskOutput on a completed background agent answers "No task found with ID" rather than a completed state. The harness''s own liveness statement for the worker was the unsolicited task-notification with <status>completed</status>, which the parent quoted in every probe reply. Documented but not exercised here: TaskOutput with block: true on a still-running task, and the parent''s /tasks listing; running, idle, and failed were not observed'
    source: probes.yaml probes[2].reply (TaskOutput description, "deprecated"); transcript.md "/tasks to see subagents"; session/*.jsonl attachment queued_command task-notification
  - id: probe.side_effect
    observed: 'none on the log: probe-activity.log had 18 lines before probe 1 and 18 after probe 4, no duplicate timestamps, and every interval in the unprobed window was 10 s (min 10, median 10, max 10). No resume or restart appears: after the probes the session holds no new subagent activity and TaskOutput errored rather than reattached. The comparison is weak, though, because the probed window contains 0 worker lines by construction (the worker had already finished)'
    source: cadence.md windows table and per-line intervals; probes.yaml log_lines_before/after; probe-activity.log
  - id: probe.post_completion
    observed: 'Probe 4 (21:57:28Z, 3m41s after the last log line): ListAgents "No reachable agents — no other Claude session is running on this machine right now" and TaskOutput(task_id a8064ebed038610db, block false, timeout 0) -> "<tool_use_error>No task found with ID: a8064ebed038610db". The finished worker is not reported as running and was not restarted; it is simply gone from the task registry'
    source: probes.yaml probes[4]; session/*.jsonl tool_result toolu_01CxCTN5F6k3jop4Ng9SRPA3 (is_error true)
