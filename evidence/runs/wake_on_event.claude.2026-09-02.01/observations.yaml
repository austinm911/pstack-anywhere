# Hand-written by the operator from the artifacts in this directory.
observations:
  - id: wake.mechanism
    observed: 'No harness wait primitive: the agent searched with ToolSearch ("select:Monitor", "wait until condition monitor watch file event", "+monitor stream events notify", "wake_on_event block until file appears wait") and wrote "No `Monitor` primitive is exposed in this session". It then used the Bash tool with run_in_background: true, timeout: 1260000, command python3 ".../scratchpad/kq_wait.py" "/private/var/folders/.../tmp.xUBQEBV8jV/event.flag" 1200, a script it wrote that blocks in kqueue (EVFILT_VNODE on the scratch directory, fflags NOTE_WRITE|EXTEND|LINK|RENAME|DELETE|ATTRIB) with a 1200 s bound, and ended its turn; Claude Code''s background-task completion is what woke it ("Background command "Block on kqueue until event.flag appears" completed (exit code 0)"). wait-call.txt line two says "shell command"'
    source: invocation.json calls[1,2,4,6] (ToolSearch), calls[8] (kq_wait.py source), calls[9] and calls[13] (run_in_background Bash); wait-accounting.yaml wait_call; transcript.md
  - id: wake.turns_during_wait
    observed: 'Zero assistant entries between the turn that entered the wait and the wake: the session log''s last assistant entry before the wait is 21:59:37.379Z (text "The kqueue wait is registered and blocking in the kernel...", herdr status done at 21:59:37.683Z) and the next is 22:00:28.006Z (the wake Bash call); the driver''s snapshots agree, assistant_entries 19 at during_wait (22:00:24.409Z) and 19 at after_wake, assistant_entries_wait_to_wake: 0, and the last usage object was unchanged across those snapshots (output_tokens 62, cache_read 55701). herdr exposes no turn or token counter (state_change_seq moved 658 -> 662 over the whole first turn only). The 19 entries are the single turn that searched for a primitive and set up the wait, before it began'
    source: session/b946c0a8-7d84-4403-a4e2-a6d4a9382bd8.jsonl assistant entry timestamps; wait-accounting.yaml snapshots.during_wait, snapshots.after_wake, assistant_entries_wait_to_wake
  - id: wake.latency
    observed: '4 s: the driver''s trigger `touch event.flag` at 22:00:24Z (trigger-at.txt, shell exited 22:00:24.433Z); the agent''s first post-wake action, Bash `date -u +%FT%TZ > woke-at.txt`, is logged at 22:00:28.006Z and woke-at.txt reads "2026-09-02T22:00:28Z" then "RETURNED: event.flag appeared (kqueue EVFILT_VNODE event on directory /private/var/folders/4n/6mnm6cw94yq_yhfvcxxft3180000gp/T/tmp.xUBQEBV8jV)". timeline.yaml records woke-at.txt as not written only because the driver read it at 22:00:24.457Z, before the agent acted; no-trigger-trial.md quotes the file as written'
    source: timeline.yaml trigger and wait_returned; drive.log 22:00:24.410Z-22:00:24.457Z; invocation.json calls[10].ts; no-trigger-trial.md "woke-at.txt after the trial, verbatim"
  - id: wake.event_kinds
    observed: 'Exercised: a file appearing in a directory, via a shell kqueue vnode watch surfaced to the parent as the completion of a backgrounded Bash task. The harness-side wake is therefore "a background shell task finishing", and by the same path a worker finishing (the task-notification seen in other runs). Documented only, not exercised: the deferred tool list at session start names CronCreate/CronList (timer), SendMessage (peer message), RemoteTrigger; no Monitor tool was available to this session ("No `Monitor` primitive is exposed in this session"). Arbitrary external signals are reachable only through a shell wrapper as here'
    source: wait-call.txt line three quoted in wait-accounting.yaml wait_call; session/*.jsonl attachment deferred_tools_delta.addedNames; transcript.md
  - id: wake.timeout_behavior
    observed: 'Not exercised. In trial 2 the agent re-launched the same kqueue wait in the background (bound 1200 s) and ended its turn at 22:00:52Z; herdr reported the prompt done, so the driver ended the trial after 28 s of its 240 s budget with still_waiting=false, and the pane was harvested and closed while the transcript still showed "1 shell still running". kq_wait.py would print "RETURNED: timeout after 1200s, no kqueue event matched; event.flag absent" and exit 2 on expiry, distinguishable from the exit-0 event path, but no timeout return was observed in this run. woke-at.txt at the end still held trial 1''s wake lines'
    source: no-trigger-trial.md (entered, budget, ended, "1 shell still running"); drive.log 22:00:24.496Z-22:00:52.579Z; invocation.json calls[8].arguments.command (timeout branch of kq_wait.py) and calls[13]
