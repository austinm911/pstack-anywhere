# Hand-written by the operator from the artifacts in this directory.
observations:
  - id: model.selection_argument
    observed: 'herdr_delegate, argument agentArgs: ["--model", "<slug>"], one call per trial, supplied by the @andrewjacop/pi-herdr extension loaded with -e (run.yaml extension; invocation.json cli.command ends "-e /Users/am/.pi/agent/npm/node_modules/@andrewjacop/pi-herdr"). The selection is passed through to the delegate harness''s own --model flag, not interpreted by pi. Trial A (calls[2]): {name: "worker-a", agent: "omp", agentArgs: ["--model", "openai-codex/gpt-5.6-luna"], prompt: <brief>}. Trial B (calls[3]): {name: "worker-b", agent: "omp", agentArgs: ["--model", "openai-codex/gpt-5.6-terra"], prompt: <brief>}. Negative (calls[7]): {name: "worker-c", agent: "omp", agentArgs: ["--model", "poteto-nonexistent-model"], prompt: <brief>}. Two earlier attempts were rejected before any worker ran: calls[0] with name "worker-A" returned "Error (VALIDATION_ERROR): agent name must start with a lowercase letter and contain only lowercase letters, digits, ''-'' or ''_'' (1-32 characters)", and calls[1] with agent: "pi" returned "Error (VALIDATION_ERROR): expected pi, detected omp", after which the parent switched every delegate to agent: "omp". The workers therefore ran on omp, not pi, and pi itself never selected a model for anything'
    source: invocation.json calls[0..3] and calls[7] arguments; session/2026-09-02T22-34-49-501Z_01a06442-b91d-7560-b043-61f3ff396a9f.jsonl toolResult entries at 22:34:59.602Z and 22:35:05.790Z; run.yaml drive.spawn_args and extension
  - id: model.harness_reported
    unsatisfiable: 'no harness-side model record for the workers is reachable from this run. pi-herdr opens each delegate pane from herdr''s own environment, the operator''s real HOME (bench/README.md "The launch is stock", last paragraph), so the workers'' session logs land outside the scratch HOME and the harness-side capture holds only the parent: usage-record.txt lists one file, the parent''s own session, with 15 "model":"gpt-5.6-luna" and 1 "modelId":"gpt-5.6-luna", and its header says so verbatim ("Only the parent''s own record is below"). The delegates were omp sessions (calls[2], [3], [7] agent: "omp"), whose logs would live under the operator''s ~/.omp/agent/sessions, not under the run''s HOME, and the run''s session/ directory holds only the parent file. For the record and not as a source: the operator looked at ~/.omp/agent/sessions/-tmp-tmp.R1WITmQj8B/ on the bench host after the run and found two omp session files, one with model_change "openai-codex/gpt-5.6-luna" and an assistant message with model "gpt-5.6-luna", the other with model_change "openai-codex/gpt-5.6-terra" and model "gpt-5.6-terra", and no third file for worker-c. Those files are the operator''s, not this run''s, and are not copied here'
  - id: model.self_report
    observed: 'Worker A (selected openai-codex/gpt-5.6-luna): line 2 "openai-codex/gpt-5.6-luna", read by the parent from the delegate''s pane via herdr_delegate''s return and herdr_read_agent (calls[5]), where the pane text shows "alpha" then "openai-codex/gpt-5.6-luna" wrapped one word per line. Worker B (selected openai-codex/gpt-5.6-terra): the parent recorded "-5", because herdr_delegate for worker-b (calls[3]) returned the literal string "-5" as the whole result and herdr_read_agent worker-b (calls[6]) returned "-5" again; self-reports.txt line 3 is "B, openai-codex/gpt-5.6-terra, -5". Worker C: no self-report, it never started. Non-authoritative, as the file''s own header says'
    source: self-reports.txt lines 1-3; session jsonl toolResult 22:35:59.949Z (worker-a delegate return), 22:36:15.748Z (worker-b delegate return "-5"), 22:36:19.579Z and 22:36:21.655Z (herdr_read_agent returns)
  - id: model.selections_differed
    observed: 'not determinable from this run''s harness-side record: harness_reported is unsatisfiable, so there is nothing authoritative to compare. The only in-run difference between the trials is the self-report, A said "openai-codex/gpt-5.6-luna" and B''s result reached the parent as "-5", which is not a model identifier and cannot be read as either confirmation or refutation. The parent''s own record shows gpt-5.6-luna only, unchanged by either spawn'
    source: usage-record.txt (one session file, 15x "model":"gpt-5.6-luna", 1x "modelId":"gpt-5.6-luna"); self-reports.txt lines 2-3
  - id: model.unknown_slug_behavior
    observed: 'neither a hard error naming the model nor a fallback: the spawn hung and timed out. herdr_delegate worker-c with agentArgs ["--model", "poteto-nonexistent-model"] (calls[7], sent 22:36:21Z) returned at 22:38:25.130Z, verbatim: "Agent in pane w1H:p3M did not finish (TIMEOUT): NOT_STARTED. Partial response from pane w1H:p3M:\n(none)". The delegate harness (omp) never reached a started state and produced no text. The parent wrote negative-trial.txt with first line "no output at all; spawn call: functions.herdr_delegate({"name":"worker-c","agent":"omp","agentArgs":["--model","poteto-nonexistent-model"],...})". The parent could distinguish this from a run only by the TIMEOUT wording, and it says nothing about the model slug, so an unknown slug is not reported as such by this path'
    source: negative-trial.txt line 1; session jsonl toolResult 22:38:25.130Z herdr_delegate; invocation.json calls[7].arguments; drive.log
