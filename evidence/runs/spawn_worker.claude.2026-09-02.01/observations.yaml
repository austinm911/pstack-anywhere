# Hand-written by the operator from the artifacts in this directory.
observations:
  - id: spawn.primitive
    observed: 'Agent, arguments {description: "Write UTC timestamp to probe.txt", prompt: "replace the contents of probe.txt with the current UTC timestamp, then report the exact line written", subagent_type: general-purpose}'
    source: invocation.json calls[0] arguments
  - id: spawn.separate_context
    observed: 'the worker has its own session file under session/<parent-id>/subagents/agent-afdae887b149395a8.jsonl, marked isSidechain: true with agentId afdae887b149395a8; its first user turn is the brief verbatim, and none of the parent prompt (no "Spawn exactly one worker", no "worker-report") appears in it'
    source: session/<parent-id>/subagents/agent-afdae887b149395a8.jsonl user messages
  - id: spawn.result_returned
    observed: 'text; the Agent call returned only "Async agent launched successfully" with the agentId, and the result arrived 8 s later as a user-role <task-notification> message injected into the parent turn, carrying <status>completed</status> and a <result> block with the worker''s full final text. No collection call by the parent'
    source: 'the parent session (session/e0de2987-....jsonl): the Agent tool_result, then the user message of type task-notification at 06:32:14.901Z'
  - id: spawn.side_effect_matches
    observed: 'probe.after.txt is exactly 2026-09-02T06:32:10Z, the line quoted inside the <result> block and inside worker-report.txt; probe.before.txt is seed'
    source: probe.before.txt, probe.after.txt, worker-report.txt, the task-notification result
  - id: spawn.parent_abstained
    observed: 'parent-side calls were Agent and one Bash heredoc writing worker-report.txt; no parent call names probe.txt. The only probe.txt write is the worker''s Bash date -u redirect'
    source: invocation.json calls[0..1] arguments and calls_workers["agent-afdae887b149395a8"]
